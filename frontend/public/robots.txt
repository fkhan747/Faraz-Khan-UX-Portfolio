User-agent: *
Allow: /

# The client case studies were vault-gated and kept out of search. The vault
# went dormant on 2026-08-02 and the pages are open, so the disallow lines for
# /case/finvista and /case/aurora came off on 2026-08-04: they were hiding the
# strongest work while the weaker unlocked pages were the only ones rankable.
# The stale /case/joat line went with them, since that slug is now a redirect
# to /case/threadfold and needs to pass its authority through.

# Pre-cleanup URLs. These now redirect to the pages that replaced them, so
# they are not worth indexing in their own right.
Disallow: /case/finvista-long

# The hidden interview deck is reached through the easter egg, not search.
Disallow: /deck/

Sitemap: https://www.khanfaraz.in/sitemap.xml
